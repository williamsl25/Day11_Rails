<h1>Add a new post</h1>

<% form_for @post do |f| %>
	<% end %>
	<div>
		<%= f.label :title %>
		<br> 
		<%= f.field :title %>
	</div>

	<div>
		<%= f.label :content %>
		<br> 
		<%= f.text_area :content %>
	</div>

	<div>
		<%= f.submit "Add a new post" %>
		<br> 
		
	</div>
	<% end %>
